# Optional: NVIDIA GPU acceleration for Windows
# Install these if you have an NVIDIA GPU but have NOT installed the full CUDA Toolkit.
# They provide the required CUDA DLLs (cublas, cudnn, cuda runtime) via pip.
# Total download: ~1.2 GB
#
# If you already have CUDA Toolkit 11.x or 12.x installed system-wide,
# these packages are not needed — GPU will be detected automatically.

nvidia-cublas-cu12>=12.0.0
nvidia-cudnn-cu12>=9.0.0
nvidia-cuda-runtime-cu12>=12.0.0
